"use client";

import { useRouter } from "next/navigation";
import Layout from "@/app/@modal/layout";

export default function Modal() {
    return (
        <Layout>
            <div>success modal</div>
        </Layout>
    );
}
